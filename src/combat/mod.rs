//! Turn manager, actions, telegraphs (optional), external support requests.

/// High-level turn-based combat state for a mission.
pub struct Mission {
    // mechs, enemies, grid, turn order, external support charges
}

impl Mission {
    pub fn new() -> Self {
        Self {}
    }
}
