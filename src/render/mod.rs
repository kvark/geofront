//! Blade scene setup, instancing for buildings/mechs, damage visuals, camera.

/// Thin wrapper around Blade engine scene for the city + mechs.
pub struct Scene {
    // handles, cameras, damage meshes
}

impl Scene {
    pub fn new() -> Self {
        Self {}
    }
}
